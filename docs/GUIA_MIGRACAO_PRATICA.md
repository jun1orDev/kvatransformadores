# 🚀 Guia Prático de Migração: Gulp → Vite

> Guia histórico da implementação. O estado final do projeto usa Vite + JavaScript modular + CSS puro.

## Resultado final

- `index.html` como entrada do Vite
- `src/main.js` para a lógica do site
- `src/style.css` para os estilos
- `public/` para imagens e fontes

## Comandos úteis

- `npm run dev`
- `npm run build`
- `npm run lint`
- `npm run preview`

## Observação

Use este arquivo apenas como memória da migração. O código atual está nos arquivos do projeto.
